#
# VLAbor Docker 環境変数設定
#
# このファイルを .env にコピーして使用してください:
#   cp .env.example .env
#

# ============================================================
# 共通設定
# ============================================================

# Unity TCP Endpoint ポート
ROS_TCP_PORT=42000

# Zenoh Bridge ポート（リモート通信用）
ZENOH_PORT=7447

# ============================================================
# ロボット設定
# ============================================================

# 左アームのシリアルポート
LEFT_SERIAL_PORT=/dev/ttyACM0

# 右アームのシリアルポート
RIGHT_SERIAL_PORT=/dev/ttyACM1

# ドライバーバックエンド (feetech / mock)
DRIVER_BACKEND=feetech

# 起動時に自動でトルクON (true / false)
AUTO_ENABLE=false

# ============================================================
# カメラ設定
# ============================================================

# カメラデバイス
CAMERA_DEVICE=/dev/video0

# カメラを有効化 (true / false)
ENABLE_CAMERA=true

# ============================================================
# Tailscale設定（リモート用）
# ============================================================

# Tailscale認証キー
# OAuth Clientから取得: https://login.tailscale.com/admin/settings/oauth
TS_AUTHKEY=

# Robot側のTailscale IP（VR側で設定）
ROBOT_TAILSCALE_IP=

# ============================================================
# アクセス制御（リモート用）
# ============================================================

# 操作を許可するVRのTailscale IP（空=全許可）
# 複数のVRユーザーがいる場合、1人だけを操作者に指定
ALLOWED_VR_IP=
