#!/bin/bash
# Simple launcher for vlabor profiles

set -e

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
VR_ROS2_DIR="$(dirname "$SCRIPT_DIR")"
ROS2_WS="$(dirname "$(dirname "$VR_ROS2_DIR")")"
LAUNCH_PID_FILE="/tmp/vr_teleop_launch.pid"

FLUENT_VISION_SRC="$ROS2_WS/src/fluent_vision_ros2"
VLABOR_SRC="$ROS2_WS/src/vlabor_ros2"

# run 冒頭: fluent_vision / vlabor を git pull し、更新があれば colcon build
update_and_build() {
    local need_build=0
    local out

    echo "[run] git pull: fluent_vision_ros2, vlabor_ros2"

    if [ -d "$FLUENT_VISION_SRC/.git" ]; then
        out=$(git -C "$FLUENT_VISION_SRC" pull 2>&1) || true
        echo "$out" | sed 's/^/  fv: /'
        if ! echo "$out" | grep -q "Already up to date"; then
            need_build=1
        fi
    fi

    if [ -d "$VLABOR_SRC/.git" ]; then
        out=$(git -C "$VLABOR_SRC" pull 2>&1) || true
        echo "$out" | sed 's/^/  vl: /'
        if ! echo "$out" | grep -q "Already up to date"; then
            need_build=1
        fi
    fi

    if [ "$need_build" -eq 1 ]; then
        echo "[run] 更新あり → colcon build --symlink-install"
        # shellcheck source=/dev/null
        source /opt/ros/humble/setup.bash 2>/dev/null || true
        (cd "$ROS2_WS" && colcon build --symlink-install) || {
            echo "[run] WARN: build に失敗しました。既存の install のまま起動します。" >&2
        }
    fi
    echo ""
}

print_usage() {
    echo "Usage: run <profile> [key:=value ...]"
    echo ""
    echo "Profiles:"
    echo "  so101_vr_dual_teleop"
    echo "  so101_single_teleop"
    echo "  so101_dual_teleop"
    echo "  vr_server_only"
    echo "  rviz_so101"
    echo "  gazebo_so101"
    echo "  overhead_camera"
    echo ""
    echo "Examples:"
    echo "  run so101_vr_dual_teleop"
    echo "  run rviz_so101 rviz_config:=/path/to/file.rviz"
}

select_profile() {
    local profiles=(
        "so101_single_teleop:単腕テレオペ(leader->follower)"
        "so101_dual_teleop:双腕テレオペ(leader->follower)"
        "so101_vr_dual_teleop:VR双腕テレオペ(標準)"
    )

    local index=0
    local key=""

    while true; do
        printf "\033[2J\033[H"
        echo "Select profile (↑/↓, Enter):"
        for i in "${!profiles[@]}"; do
            local name="${profiles[$i]%%:*}"
            local desc="${profiles[$i]#*:}"
            if [ "$i" -eq "$index" ]; then
                printf "> %s  - %s\n" "$name" "$desc"
            else
                printf "  %s  - %s\n" "$name" "$desc"
            fi
        done

        IFS= read -rsn1 key
        if [ "$key" = "" ]; then
            PROFILE="${profiles[$index]%%:*}"
            return
        fi
        if [ "$key" = $'\x1b' ]; then
            read -rsn2 key
            case "$key" in
                "[A")
                    if [ "$index" -gt 0 ]; then
                        index=$((index - 1))
                    fi
                    ;;
                "[B")
                    if [ "$index" -lt $((${#profiles[@]} - 1)) ]; then
                        index=$((index + 1))
                    fi
                    ;;
            esac
        fi
    done
}

setup_ros2() {
    if [ -f /opt/ros/humble/setup.bash ]; then
        source /opt/ros/humble/setup.bash
    else
        echo "ROS2 Humble not found" >&2
        exit 1
    fi

    if [ -f "$ROS2_WS/install/setup.bash" ]; then
        source "$ROS2_WS/install/setup.bash"
    else
        echo "Workspace not built: $ROS2_WS" >&2
        exit 1
    fi

    if [ -n "$PYTHONPATH" ]; then
        PYTHONPATH="$(echo "$PYTHONPATH" | tr ':' '\n' | grep -v 'python3.13' | paste -sd ':' -)"
    fi
    export PYTHONPATH="/home/takatronix/ros2_ws/install/vr_haptic_msgs/local/lib/python3.10/dist-packages:/home/takatronix/.local/lib/python3.10/site-packages${PYTHONPATH:+:$PYTHONPATH}"
}

setup_display() {
    if [ -z "${DISPLAY:-}" ]; then
        export DISPLAY=":0"
    fi
    if [ -z "${XAUTHORITY:-}" ] && [ -f "$HOME/.Xauthority" ]; then
        export XAUTHORITY="$HOME/.Xauthority"
    fi
}

GREEN='\033[0;32m'
CYAN='\033[0;36m'
NC='\033[0m'

# 表示用ホスト名（vlabor.local）と、外部から見たIPを取得
HOST_NAME="vlabor.local"
# IPv4 のみ取得（IPv6を出さない）
get_external_ip() {
    local ip
    ip=$(getent ahostsv4 vlabor.local 2>/dev/null | awk '{print $1; exit}')
    if [ -z "$ip" ]; then
        ip=$(ip -4 route get 1 2>/dev/null | awk '{print $7; exit}')
    fi
    if [ -z "$ip" ]; then
        ip=$(hostname -I 2>/dev/null | tr ' ' '\n' | grep -v ':' | head -1)
    fi
    echo "$ip"
}

open_browser() {
    local urls=()
    case "$PROFILE" in
        so101_vr_dual_teleop|so101_dual_teleop)
            urls=("http://${HOST_NAME}:8888" "http://${HOST_NAME}:8080" "http://${HOST_NAME}:8081") ;;
        so101_single_teleop)
            urls=("http://${HOST_NAME}:8888" "http://${HOST_NAME}:8080") ;;
        overhead_camera)
            urls=("http://${HOST_NAME}:8888") ;;
        *)  return ;;
    esac

    sleep 3

    for url in "${urls[@]}"; do
        if ssh -o ConnectTimeout=2 -o BatchMode=yes mac "open '$url'" 2>/dev/null; then
            echo -e "${GREEN}🌐 Opened: $url${NC}"
        fi
    done

    local ext_ip
    ext_ip=$(get_external_ip)
    echo ""
    echo -e "${CYAN}========================================${NC}"
    echo -e "${CYAN}  Web UI URLs (${HOST_NAME})${NC}"
    echo -e "${CYAN}========================================${NC}"
    for url in "${urls[@]}"; do
        echo -e "  ${GREEN}$url${NC}"
    done
    if [ -n "$ext_ip" ]; then
        echo -e "  外部からアクセス: ${GREEN}http://${ext_ip}:8888${NC} など (同じポート)"
        echo -e "  vlaborアプリからは、IPアドレス ${GREEN}${ext_ip}${NC} ポートは ${GREEN}42000${NC} で接続してください"
    fi
    echo -e "${CYAN}========================================${NC}"
    echo ""
}

if [ "$1" = "-h" ] || [ "$1" = "--help" ]; then
    print_usage
    exit 0
fi

update_and_build

if [ $# -lt 1 ]; then
    select_profile
else
    PROFILE="$1"
    shift
fi

setup_ros2
setup_display

trap 'rm -f "$LAUNCH_PID_FILE"' EXIT

ros2 launch vlabor_launch vlabor.launch.py profile:=${PROFILE} "$@" &
launch_pid=$!
echo "$launch_pid" > "$LAUNCH_PID_FILE"

open_browser &

wait "$launch_pid"
